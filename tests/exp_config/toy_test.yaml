model:
  vocab_size: 64
  context_length: 8
  num_layers: 4
  num_heads: 2
  d_model: 32
  d_ff: 86

runtime:
  max_steps: 1000
  batch_size: 16
  eval_interval: 10
  log_interval: 2
  checkpoint_interval: 50
